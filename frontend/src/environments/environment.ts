export const environment = {
  production: false,
  // Base URL of the CleanStart API. No mock-data fallback — if this is left empty,
  // ApiService throws immediately with a clear message instead of the app silently
  // behaving as if a backend were there.
  apiBaseUrl: 'http://localhost:5000',
};
