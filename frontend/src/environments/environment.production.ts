export const environment = {
  production: true,
  // Set this to your deployed CleanStart API URL before building for production
  // (or wire it up via a build-time env var / CI replacement step).
  apiBaseUrl: '',
};
